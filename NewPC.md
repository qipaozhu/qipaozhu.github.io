# 激活前和验机
验机注意验外观是否正常无痕迹，键盘按键是否正常，电脑是否处于运输模式
## 1.跳过联网
- 在系统提示联网的界面，按下键盘上的 `Shift` + `F10` ，打开命令提示符
- 方法1：`start ms-cxh:localonly`
- 方法2：`oobe\bypassnro`
## 2.设置英文用户名
中文和空格有些软件会出问题，有空格微信也会出问题（使用看起来还好）
## 3.图吧工具箱
使用U盘复制到电脑，打开后检查电脑配置是否相符，屏幕是否有坏点，硬盘通电时间（CrystalDiskinfo）

# 进入系统后设置
先配置好电脑，再安装微信QQ等常用软件
## 日常注意的事项
1. 每个软件安装的时候，把默认的安装路径`C:\Program Files\xxxx`改成
`D:\Program Files\xxxx`（直接在输入框把C改成D，如果不行只能手动）
2. 每个软件安装后设置配置和缓存到其他路径，缓存可以为`D:\Temp\xxxx`，
    - 微信在设置->存储位置可以改，默认在D盘可以不改，如果默认是C盘可以改成`D:\<你的用户名>\Documents\xwechat_files`这样
    - QQ在设置->存储管理，下载路径可以设置为`D:\<你的用户名>\Download`，聊天信息默认保存到路径设置为`D:\<你的用户名>\Documents\QQ_files`
3. 卸载在设置App里面卸载，不要只删除桌面快捷方式
4. 定期检查任务管理器中的启动页面，不需要的软件要禁用自启动

## 在设置App部分（视觉部分）
在电脑外观上进行的优化，
### 个性化->背景
- 设置为图片
### 个性化->锁屏界面
- 个性化设置背景 => `图片`
- 显示花絮、提示技巧等 => `关`
- 锁屏界面状态 => `无`
### 个性化->主题->桌面图标设置
- 勾选 `计算机` `回收站` 
- **可选** 勾选 `网络`
### 个性化->任务栏
- 搜索 =>`隐藏`
- 小组件 => `关`
### 系统->屏幕->显示卡->默认图形设置
- 硬件加速GPU计划 => `开`
### 系统->屏幕->高级显示设置
- **可选** 刷新率改为电脑最高支持的

## 在设置App部分（功能部分）
### 时间和语言->语言和区域
1. 点击 `添加语言`
2. 下拉找到 `英语（美国）`，点击下一页
3. 取消勾选所有 `可选语言功能`
4. 点击 `安装`
### 系统->存储->保存新内容的地方
- 全部改成**D**盘
### 系统->存储
- **建议** 存储感知 => `开`
### 隐私和安全性->设备加密
- 设备加密 => `关`

## Windows文件资源管理器设置
打开文件资源管理器（就是桌面那个此电脑）
### 显示文件扩展名
找到上面工具栏，点击`查看`>`显示`>`文件扩展名`，勾选
### 更改桌面和下载文件夹位置
一直点击上面导航栏**向上箭头**的，到一个叫桌面文件夹的 <br>
找到桌面文件夹，右键点击`属性`，打开`位置`选项卡。点击`移动`。建议移动到`D:\<你的用户名>\desktop`文件夹。然后点击`应用` <br>
下载文件夹同理，放在`D:\<你的用户名>\Download`文件夹。建议检查下其他文件夹是否都是在D盘
> 注意
> ---
> 如果提示**是否将所有文件从原位置移动到新位置**，选择**是**

## 使用Win11Debloat工具优化
运行命令：`& ([scriptblock]::Create((irm "https://debloat.raphi.re/")))`
> 可以根据下面步骤或者使用我提供的配置文件，在右上角三个横杆点击`Import config`导入我发的配置 [点击下载配置](./Win11Debloat-Config.json)
1. 点击`Custom Setup`
2. App Removal 看情况选，大部分可以全不选。要选建议把所有Bing、AI功能删掉
    - 我的配置会卸载部分无用软件
3. 点击`Next`，在System Tweaks中，
    - 先点击**Clear Selection**，再点击**Quick Select**，勾选里面所有东西（确保前面箭头都是**打勾**而不是横线）
    - **Start Menu & Search**中，`Hide recommend section什么什么` `Disable Phone Link什么什么`勾选上
    - **File Explorer**中，`Open File Explorer to`改成`This PC`

4. 点击`Next`，建议取消勾选`Create a system restore point`，然后点击`Apply Changes`

## Edge浏览器设置
### 设置->系统和性能->系统
- 启动增强 => `关`
- 关闭……继续运行扩展 => `关`
### 设置->系统和性能->性能
- 预加载页面以更快…… =>`关`
### 下载
- 位置 =>  选个合适的，根据上面`D:\<你的用户名>\Download`设置
- **可选** 在浏览器中打开Office文件 => `关`
### 新建标签页->设置图标
- 先全部关闭
- 如果你个人觉得**太单调**可以
  - 背景 => `开`
  - 快速链接 => `开`
  - 显示推广链接 => `关`

# 安装软件
## 杀毒软件
银狐木马很多，不装容易下到木马。杀毒软件可以拦截假网站，一定程度上可以防范病毒。**注意**下载杀毒软件时的银狐木马网站，建议下载都**从下面链接**下载
### 个人推荐 360安全卫士极速版
注意是**极速版**，不能是普通版，普通版可能诱导全家桶。使用时只要注意下平时不要直接关闭他的少量广告弹窗，在下拉箭头里要选择**永不弹出**。360安全浏览器也要注意不要选到

- 一键上网、极速搜索、360桌面助手、开机小助手等等全部卸载或关闭。可以在功能大全卸载或者在设置页面关闭。
- AI办公也要关闭，在360安全卫士设置里面寻找关闭按钮
- 功能大全->主页防护中，选择默认浏览器为Edge并锁定。默认主页需要**全部取消**锁定，然后再去Edge浏览器里面改主页，改为新建标签页
> 极速版下载链接： https://dl.360scdn.com/setupbeta_jisu.exe
### 纯净选 火绒安全
火绒安全省心，无广告，轻量，一般够用。能力比360稍弱，无需有小心思对付
> 官网链接： https://www.huorong.cn
### 新秀 悟空安全 - 360的杀毒能力，但纯净
360出品，特别纯净无广告（个人没试过）
> 官网链接： https://www.wukongsafe.cn


# 其他网上说的，我的观点
这些都是网上收集的教程，我认为这些没用或者不好用，列在这里
## 不要关闭用户账户控制
关闭后虽然有时候不会弹窗是否管理员运行，但是大部分病毒需要管理员才能运行，开启后可以一定程度上防病毒
## 不建议分区
电脑两个区就足够使用，分太多可能出现D盘也满，最后文件都要放在E盘情况出现
## Internet属性里面的启用弹窗窗口阻止程序
广告该弹还是弹，开了也没用，那是针对IE浏览器早期的广告
## 不要关闭Windows自动更新
Windows有一些严重的漏洞，需要不断更新来修复。如果实在烦也不要彻底禁用，可以设置暂停几天
## 不要不安装杀毒软件
自带Defender很难用，而且杀毒效果差
